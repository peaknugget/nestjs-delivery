apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx-app
    type: back-end
  type: NodePort
  ports:
    - port: 80 # Service port
      targetPort: 80 # Container port
      nodePort: 30080
