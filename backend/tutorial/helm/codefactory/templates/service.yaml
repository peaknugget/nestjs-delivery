apiVersion: v1
kind: Service
metadata:
  name: {{ include "codefactory.fullname" . }}
  labels:
    {{- include "codefactory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codefactory.selectorLabels" . | nindent 4 }}
