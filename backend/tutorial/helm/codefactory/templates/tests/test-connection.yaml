apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codefactory.fullname" . }}-test-connection"
  labels:
    {{- include "codefactory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "codefactory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
